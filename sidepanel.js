// Side panel script for detailed capsule view

document.getElementById('close-btn').addEventListener('click', () => {
  window.close();
});

// Load capsule details
// This would be populated based on the selected capsule
